/**
 * Empty on purpose. This is where the live-coding solution goes.
 *
 * <p>The katas next door are reference material to read before the call, not code to import during
 * it. Copying a kata into an answer produces a solution shaped like the wrong problem; the point of
 * having written them is that the patterns are in your fingers, not on your clipboard.
 *
 * <p>Start with the test, name it after the behaviour the interviewer described, and let the API
 * fall out of what the test needed to say.
 */
package io.github.tabmadi.interview;
